SETTINGS = {
    -- Commands/Controls are already binded for players, bind descriptions and other stuff about binds can be changed in client/bindings.lua

    Marker = {
        Type = 0,
        Color = {
            r = 100,
            g = 250,
            b = 100,
            a = 150,
        },
    },

    Texts = {
        SelectionHelpText = "Use ~INPUT_FRONTEND_UP~ ~INPUT_FRONTEND_DOWN~ for target selection.\nTo cancel selection press ~INPUT_FRONTEND_RRIGHT~\nTo confirm selection press ~INPUT_FRONTEND_RDOWN~"
    },
}